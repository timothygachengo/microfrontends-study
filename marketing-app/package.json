{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "npx lerna run start",
    "build": "npx lerna run build",
    "graph": "nx graph"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
